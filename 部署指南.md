# 个人学术主页 — 服务器部署指南

本项目为纯静态网站（HTML/CSS/JS），已托管于 GitHub Pages (`chhy-bit.github.io`)，同时支持部署到自有 Ubuntu 22.04 服务器。

---

## 架构概览

```
Windows 本地 (e:\ChhY-bit.github.io)
    │
    │  .\deploy.ps1  (tar 打包 → SSH 管道 → 服务器解压)
    │
    ▼
Ubuntu 22.04 (193.112.175.76)
    │
    ├── Nginx (端口 80)
    └── /var/www/personal-site/
```

---

## 文件说明

| 文件 | 用途 | 运行位置 |
|------|------|----------|
| `server-setup.sh` | 首次安装 Nginx + 创建站点配置 | 服务器上（仅需一次） |
| `deploy.ps1` | 打包上传网站文件 | Windows 本地（每次更新） |

---

## 一、首次配置（仅需执行一次）

### 1.1 上传配置脚本到服务器

在 Windows PowerShell 中执行：

```powershell
scp server-setup.sh ubuntu@193.112.175.76:~/
```

### 1.2 SSH 登录服务器并运行

```powershell
ssh ubuntu@193.112.175.76
bash server-setup.sh
exit
```

该脚本会自动完成：
- 安装 Nginx
- 创建 `/var/www/personal-site` 目录
- 配置 Nginx（Gzip 压缩、静态缓存、安全响应头）
- 启动 Nginx 服务

### 1.3 验证

浏览器打开 `http://193.112.175.76`，应看到 Nginx 默认页面（此时尚未上传网站文件）。

---

## 二、日常部署（每次网站更新后执行）

### 在 Windows PowerShell 中运行：

```powershell
cd E:\ChhY-bit.github.io
.\deploy.ps1
```

输入一次服务器密码，脚本会自动：
1. 用 `tar` 打包本地文件
2. 通过 SSH 管道一次性上传到服务器
3. 清空旧文件、解压新文件
4. 设置权限（755，nginx 可读）
5. 重载 Nginx

### 部署结果

访问 `http://193.112.175.76` 即可看到更新后的网站。

---

## 三、目录结构与 SSL 说明

### 3.1 网站目录结构（服务器上）

```
/var/www/personal-site/
├── index.html
├── css/
├── js/
├── images/
├── achievements/
├── awards/
└── docs/
```

文件属主为 `ubuntu`，权限 `755`（nginx 以 `www-data` 身份通过 world-readable 读取）。

### 3.2 Nginx 配置

配置文件位于 `/etc/nginx/sites-available/personal-site`，主要特性：
- 静态资源（jpg/png/css/js/json）缓存 7 天
- HTML 文件不缓存
- Gzip 压缩开启
- 安全头：X-Frame-Options、X-Content-Type-Options、XSS-Protection

修改配置后重载：
```bash
sudo nginx -t && sudo systemctl reload nginx
```

### 3.3 可选：配置 HTTPS

若绑定了域名，在服务器上执行：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

Certbot 会自动申请 Let's Encrypt 证书并更新 Nginx 配置。

---

## 四、故障排查

| 现象 | 原因 | 解决 |
|------|------|------|
| `Permission denied` | 父目录属主错误 | SSH 登录执行 `sudo chown ubuntu:ubuntu /var/www/personal-site` |
| `gzip: stdin: not in gzip format` | PowerShell 管道破坏二进制流 | 确认使用 `cmd /c` 方式执行（当前脚本已处理） |
| 上传成功但页面不更新 | 浏览器缓存 HTML | Ctrl+F5 强制刷新，或等待缓存过期 |
| Nginx 报 403 Forbidden | 文件权限错误 | 执行 `sudo chmod -R 755 /var/www/personal-site` |
| 无法连接服务器 | Nginx 未运行或防火墙 | `sudo systemctl status nginx`，检查安全组/防火墙 80 端口 |
